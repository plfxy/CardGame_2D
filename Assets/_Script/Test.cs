using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class Test : MonoBehaviour {

	// Use this for initialization
	/*void Start () {
		CardPile cp = new CardPile();
		CardDrawer cd = new CardDrawer(new OrderlyCardGetter(),new CardReturner(true,true));
		List<int> l=new List<int>();

		cp.AddCard (new Card ("233"));
		cp.AddCard (new Card ("123"));
		cp.AddCard (new Card ("323"));
		cp.AddCard (new Card ("423"));
		cp.Shuffle ();

		GetComponent<Text> ().text = cd.DrawCard(cp,1)[0].info;
	}
	
	// Update is called once per frame
	void Update () {
		
	}*/
}
